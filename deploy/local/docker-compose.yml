name: deputies

# Default local stack: all-in-one control plane with API + workers in one process.
# Use docker-compose.split.yml to run API, workers, and Docker orchestration separately.
include:
  - docker-compose.all.yml
